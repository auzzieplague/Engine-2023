#pragma once

enum class TextureFilter {
    Nearest,  // Nearest-neighbor filtering
    Linear    // Linear interpolation filtering
};

enum class TextureWrap {
    Repeat,   // Repeat the texture
    Clamp     // Clamp texture coordinates to the edge
};


class Texture {
public:
//    Texture();
//    ~Texture();
//
//    void initialize(const unsigned char* image_data, int width, int height, int channels);
//    void bind();
//    void unbind();
//    void setFiltering(TextureFilter filter);
//    void setWrapping(TextureWrap wrap);
//    void release();
};
